#:schema https://pitchfork.jdx.dev/schema.json

# The daemon namespace derives from the directory name. Each Worktrunk worktree
# gets isolated daemons with its own ports from mise-tasks/setup.

[daemons.dev]
run = "exec vp dev"
mise = true
auto = ["start", "stop"]
retry = 3
ready_output = "Local:"
